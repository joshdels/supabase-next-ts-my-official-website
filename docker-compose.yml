version: "3"
services:
  app:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app              # Mount current project for live edits
      - /app/node_modules   # Avoid overwriting node_modules
    ports:
      - "3000:3000"
    command: npm run dev
