services:
  app:
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: development
    command: sh -c "npm install && npm run dev -H 0.0.0.0"
